apply plugin: 'io.gitlab.arturbosch.detekt'

detekt {
    buildUponDefaultConfig = true
    allRules = false
    config.setFrom("$project.rootDir/tools/default-detekt-config.yml")
    baseline = file("$project.rootDir/detekt/$projectDir/baseline.xml")
}
